# Быстрый старт с Telegram ботом (локальная разработка)

## Шаг 1: Установка туннеля

### Для РФ (рекомендуется): pingy

1. Установите:
   ```bash
   npm install -g pingy-cli
   ```

### Альтернативы (если доступны):

**ngrok:**
1. Скачайте ngrok: https://ngrok.com/download
2. Зарегистрируйтесь на https://dashboard.ngrok.com
3. Настройте:
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```

**Российские аналоги:**
- xTunnel: https://xtunnel.ru
- Tuna: https://tuna.ru (если доступен)

## Шаг 2: Создание бота

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Сохраните токен бота

## Шаг 3: Запуск туннеля

### Вариант 1: xTunnel (рекомендуется для РФ)

1. Зарегистрируйтесь на https://xtunnel.ru
2. Скачайте клиент или используйте веб-интерфейс
3. Создайте туннель на порт 5173
4. Скопируйте HTTPS URL (например: `https://ваш-домен.xtunnel.ru`)

### Вариант 2: pingy (если доступен)

1. Установите глобально:
   ```bash
   npm install -g pingy-cli
   ```
2. Запустите:
   ```bash
   pingy dev 5173
   ```
   или попробуйте:
   ```bash
   pingy start --port 5173
   ```
3. Если URL не показывается, проверьте вывод команды - он должен быть в консоли

### Вариант 2: ngrok (если доступен)

1. Установите ngrok: https://ngrok.com/download
2. Зарегистрируйтесь: https://dashboard.ngrok.com
3. Получите authtoken и настройте:
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```
4. Запустите:
   ```bash
   ngrok http 5173
   ```
5. Скопируйте HTTPS URL (например: `https://abc123.ngrok-free.app`)

### Вариант 3: cloudflared (если доступен)

1. Установите cloudflared: https://github.com/cloudflare/cloudflared/releases
2. Запустите:
   ```bash
   cloudflared tunnel --url http://localhost:5173
   ```
3. Скопируйте HTTPS URL

### ⚠️ Не используйте localtunnel для Telegram WebApp!

Localtunnel показывает страницу-предупреждение, которую Telegram не может обойти.

## Шаг 4: Настройка BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. `/mybots` → выберите вашего бота
3. "Bot Settings" → "Menu Button" → "Configure Menu Button"
4. Текст: `Открыть приложение`
5. URL: вставьте ваш туннель URL (pingy, ngrok или cloudflared, например: `https://abc123.pingy.io`)

## Шаг 5: Настройка .env

В `project_finance_backend/.env`:
```env
telegram_bot_token=YOUR_BOT_TOKEN_FROM_BOTFATHER
telegram_webapp_url=https://abc123.pingy.io
```

**Важно:** URL должен совпадать с тем, что вы указали в BotFather!

## Шаг 6: Установка зависимостей

```bash
cd project_finance_backend
poetry install
```

## Шаг 7: Запуск

**Терминал 1** (Туннель):
```bash
# Если pingy (рекомендуется для РФ):
pingy dev 5173
# или
pingy start 5173

# Если ngrok:
ngrok http 5173

# Если cloudflared:
cloudflared tunnel --url http://localhost:5173
```
⚠️ Не закрывайте этот терминал!

**Терминал 2** (Backend):
```bash
cd project_finance_backend
poetry run python run.py
# или
poetry run uvicorn app.main:app --reload
```

**Терминал 3** (Frontend):
```bash
cd project_finance_front
npm run dev
```

## Шаг 8: Тестирование

1. Откройте вашего бота в Telegram
2. Отправьте `/start`
3. Нажмите кнопку "Открыть приложение"
4. Приложение должно открыться и автоматически авторизовать вас!

## Важные замечания

⚠️ **При каждом перезапуске туннеля URL меняется!**
- Обновите URL в BotFather
- Обновите URL в `.env`

💡 **Для постоянного URL:**
- Используйте платный план ngrok с статическим доменом
- Или используйте cloudflared с постоянным доменом
- Pingy обычно дает стабильный URL на сессию

❌ **Не используйте localtunnel!**
- Показывает страницу-предупреждение
- Telegram WebApp не может её обойти

✅ **Для РФ рекомендуется xTunnel:**
- Работает без блокировок
- Стабильный сервис
- Без страниц-предупреждений
- Подробнее: см. TUNNEL_ALTERNATIVES.md

