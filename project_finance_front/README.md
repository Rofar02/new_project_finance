# Finance Tracker - Telegram Mini App

iOS-стильное Telegram Mini App для финансового трекера с интеграцией в FastAPI бэкенд.

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка
- **Tailwind CSS** - стилизация в стиле iOS dark mode
- **Axios** - HTTP запросы
- **Recharts** - графики и диаграммы
- **Framer Motion** - анимации
- **Lucide React** - иконки
- **React Router** - навигация

## 📋 Требования

- Node.js 18+
- npm или yarn
- Запущенный FastAPI бэкенд на `http://127.0.0.1:8000`

## 🛠️ Установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev сервер:
```bash
npm run dev
```

3. Откройте в браузере: `http://localhost:5173`

## 📱 Использование в Telegram

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Настройте Web App URL на ваш хостинг
3. Откройте бота в Telegram и запустите Mini App

## 🎨 Особенности дизайна

- **iOS 17 dark mode** стиль
- Фиолетовая цветовая схема (#8E44FD)
- Скруглённые углы и blur эффекты
- Плавные анимации
- Mobile-first подход
- Адаптация под Telegram WebApp SDK

## 📄 Экраны

1. **Login** - Авторизация по email/password
2. **Register** - Регистрация нового пользователя
3. **Dashboard** - Главный экран с балансом и графиками
4. **Transactions** - Список транзакций с swipe-действиями
5. **AddTransaction** - Форма добавления транзакции

## 🔌 API Интеграция

Приложение работает с FastAPI бэкендом:

- `POST /token` - логин (OAuth2 форма)
- `POST /users/` - регистрация
- `GET /transactions/` - список транзакций
- `POST /transactions/` - создать транзакцию
- `GET /categories/` - список категорий

Токен автоматически передаётся в заголовке: `Authorization: Bearer {token}`

## 📁 Структура проекта

```
src/
├── api/              # API слой
│   ├── auth.ts
│   ├── transactions.ts
│   ├── categories.ts
│   └── config.ts
├── components/       # Компоненты
│   ├── ios/         # iOS-стиль компоненты
│   └── shared/      # Общие компоненты
├── contexts/        # React контексты
│   └── AuthContext.tsx
├── hooks/           # Кастомные хуки
│   ├── useAuth.ts
│   ├── useTransactions.ts
│   └── useTelegram.ts
├── pages/           # Страницы приложения
│   ├── Login.tsx
│   ├── Register.tsx
│   ├── Dashboard.tsx
│   ├── Transactions.tsx
│   └── AddTransaction.tsx
├── styles/          # Стили
│   └── index.css
├── types/           # TypeScript типы
│   └── index.ts
├── utils/           # Утилиты
│   └── telegram.ts
├── App.tsx          # Главный компонент
└── main.tsx         # Точка входа
```

## 🎯 Функционал

- ✅ Авторизация/регистрация с валидацией
- ✅ Просмотр списка транзакций (группировка по датам)
- ✅ Добавление транзакции (сумма, категория, описание)
- ✅ Удаление транзакции (swipe left)
- ✅ Графики: круговая диаграмма по категориям, линейный график динамики
- ✅ Отображение баланса и статистики
- ✅ Интеграция с Telegram WebApp SDK
- ✅ Сохранение токена в Telegram.CloudStorage

## 🔧 Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📝 Примечания

- Токен сохраняется в `localStorage` и `Telegram.CloudStorage`
- Приложение автоматически определяет, открыто ли оно в Telegram
- Все запросы к API автоматически включают токен авторизации
- При ошибке 401 происходит автоматический logout

## 🐛 Решение проблем

**CORS ошибки:**
Убедитесь, что FastAPI бэкенд настроен для работы с фронтендом (CORS middleware).

**Токен не сохраняется:**
Проверьте, что Telegram WebApp SDK загружен (должен быть в `index.html`).

**Графики не отображаются:**
Убедитесь, что есть данные транзакций в бэкенде.

## 📄 Лицензия

MIT








