# Отладка черного экрана

## Шаги для диагностики:

1. **Открой консоль браузера (F12)**
   - Перейди на вкладку "Console"
   - Посмотри, есть ли ошибки (красным цветом)

2. **Проверь логи:**
   - Должны быть сообщения: `🚀 Starting app...`, `✅ Root element found...`, `📱 App component rendering...`
   - Если их нет - проблема с загрузкой модулей

3. **Проверь вкладку "Network":**
   - Убедись, что все файлы загружаются (статус 200)
   - Если есть ошибки 404 - файлы не найдены

4. **Возможные проблемы:**

   **Проблема: Ошибки в консоли**
   - Скопируй текст ошибки
   - Проверь, что все зависимости установлены: `npm install`

   **Проблема: Бесконечная загрузка**
   - AuthContext может зацикливаться
   - Попробуй очистить localStorage: в консоли выполни `localStorage.clear()`
   - Обнови страницу

   **Проблема: "Cannot find module"**
   - Убедись, что все зависимости установлены
   - Запусти: `npm install`
   - Перезапусти dev сервер: `npm run dev`

   **Проблема: CORS ошибки**
   - Это нормально, если бэкенд не запущен
   - Приложение должно показать страницу логина даже без бэкенда

5. **Быстрый тест:**
   - Открой `http://localhost:5173` в браузере
   - Должна появиться страница логина (даже если бэкенд не запущен)
   - Если видишь черный экран - проверь консоль

## Если ничего не помогает:

1. Останови dev сервер (Ctrl+C)
2. Удали `node_modules` и `package-lock.json`
3. Запусти `npm install`
4. Запусти `npm run dev`
5. Открой `http://localhost:5173` в режиме инкогнито








